{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(ctest) launch",
            "type": "lldb",
            "request": "launch",                     
            "program": "${cmake.testProgram}",
            "args": [ "${cmake.testArgs}" ],
            "cwd": "${cmake.testWorkingDirectory}",     
            "preRunCommands": [
                "breakpoint set --name main"
            ]
        }
    ]
}
